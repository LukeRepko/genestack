apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# NOTE: Do NOT set namespace here - Helm templates already specify namespace
# and Helm's --create-namespace handles namespace creation.
# Setting namespace here causes ownership conflicts with Helm.

resources:
  - ../base
